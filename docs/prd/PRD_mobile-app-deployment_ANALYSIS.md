# PRD Analysis Report: 모바일 앱 배포

## 분석 대상
- **문서**: `PRD: WIGTN Spot Finder 모바일 앱 배포`
- **버전**: 1.0
- **분석일**: 2026-01-31

---

## 요약

| 카테고리 | 발견 | Critical | Major | Minor |
|----------|------|----------|-------|-------|
| 완전성 | 6 | 2 | 3 | 1 |
| 실현가능성 | 4 | 1 | 2 | 1 |
| 보안 | 4 | 2 | 2 | 0 |
| 일관성 | 2 | 0 | 1 | 1 |
| **총계** | **16** | **5** | **8** | **3** |

---

## 상세 분석

### 🔴 Critical (즉시 수정 필요)

#### C-1. 푸시 알림 백엔드 엔드포인트 미존재
- **위치**: Section 5.3 - 백엔드 추가 엔드포인트
- **문제**: PRD에 `POST /api/v1/push/register`, `POST /api/v1/push/send` 명시했으나, 현재 백엔드에 해당 코드 없음 (`notifications*.py` 파일 미존재)
- **영향**: MVP 기능인 푸시 알림 구현 불가
- **개선안**:
  ```
  src/api/routes/notifications.py 신규 생성 필요:
  - POST /api/v1/push/register (토큰 저장)
  - POST /api/v1/push/send (Expo Push API 연동)
  - 푸시 토큰 저장용 DB 테이블 필요
  ```

#### C-2. 푸시 토큰 저장소 미정의
- **위치**: Section 5.3
- **문제**: 푸시 토큰을 어디에 저장할지 정의 없음 (PostgreSQL? Redis?)
- **영향**: 스케일링 시 토큰 관리 혼란
- **개선안**:
  ```sql
  CREATE TABLE push_tokens (
    id UUID PRIMARY KEY,
    user_id UUID REFERENCES users(id),
    token VARCHAR(255) NOT NULL UNIQUE,
    platform VARCHAR(10) CHECK (platform IN ('ios', 'android')),
    created_at TIMESTAMP DEFAULT NOW(),
    last_used_at TIMESTAMP
  );
  ```

#### C-3. Naver Maps React Native SDK 미지원
- **위치**: Section 2 - 지도 (iframe)
- **문제**: "react-native-maps + Naver Maps 딥링크"로 명시했으나, Naver Maps는 공식 React Native SDK 없음
- **영향**: 지도 기능 구현 방식 재검토 필요
- **개선안**:
  - 옵션 1: Google Maps 사용 (react-native-maps 기본 지원)
  - 옵션 2: Naver Maps WebView embed
  - 옵션 3: Naver Maps 앱 딥링크 (`nmap://` scheme)

#### C-4. OAuth 리다이렉트 URI 미등록
- **위치**: Section 6 - 앱스토어 요구사항
- **문제**: Supabase Auth 설정에 모바일 리다이렉트 URI 등록 절차 누락
- **영향**: OAuth 로그인 실패
- **개선안**:
  ```
  Supabase Dashboard > Auth > URL Configuration에 추가:
  - spotfinder://auth/callback
  - https://spotfinder.app/auth/callback (Universal Links)

  Google Cloud Console OAuth 설정:
  - iOS: Bundle ID 등록
  - Android: SHA-1 fingerprint 등록

  Kakao Developers:
  - iOS/Android 앱 키 등록
  ```

#### C-5. 개인정보처리방침 URL 미정의
- **위치**: Section 6 - 앱스토어 요구사항
- **문제**: "개인정보처리방침, 이용약관" 언급만 있고 실제 URL/호스팅 계획 없음
- **영향**: 앱스토어 심사 거절 (필수 항목)
- **개선안**:
  ```
  - 개인정보처리방침: https://spotfinder.app/privacy
  - 이용약관: https://spotfinder.app/terms
  - 호스팅: Vercel 프론트엔드에 정적 페이지 추가
  ```

---

### 🟡 Major (구현 전 수정 권장)

#### M-1. Expo SDK 버전 미명시
- **위치**: Section 1.3 - 추천 기술 스택
- **문제**: Expo 버전 명시 없음 (SDK 51? 52?)
- **영향**: 의존성 충돌, 라이브러리 호환성 이슈
- **개선안**: `Expo SDK 52` (최신 LTS) 명시, react-native 0.76+ 요구

#### M-2. 오프라인 캐싱 전략 상세 미정의
- **위치**: Section 3 Phase 4
- **문제**: "오프라인 캐싱 전략" 언급만 있고 구체적 구현 방안 없음
- **영향**: 지하철, 터널 등에서 앱 사용 불가
- **개선안**:
  ```
  캐싱 전략:
  1. 팝업 목록: AsyncStorage + 1시간 TTL
  2. 팝업 이미지: expo-file-system + 로컬 캐시
  3. 지도 타일: react-native-maps 기본 캐싱
  4. API 응답: @tanstack/react-query 캐싱
  ```

#### M-3. 앱 버전 관리 전략 미정의
- **위치**: 전체
- **문제**: 앱 버전 관리, 강제 업데이트, OTA 정책 없음
- **영향**: 구버전 호환성 이슈, 보안 취약점 노출
- **개선안**:
  ```
  버전 관리:
  - Semantic Versioning (1.0.0)
  - 최소 지원 버전 API (/api/v1/app/version)
  - OTA: expo-updates로 마이너 업데이트
  - 스토어: 메이저 업데이트만 제출
  ```

#### M-4. 에러 핸들링/로깅 미정의
- **위치**: Section 8 - 검증 방법
- **문제**: 모바일 앱 크래시 리포팅, 로깅 전략 없음
- **영향**: 프로덕션 이슈 디버깅 불가
- **개선안**:
  ```
  - Sentry React Native SDK 설치
  - expo-error-boundary 설정
  - 로그: Logtail 또는 Better Stack 연동
  ```

#### M-5. 테스트 전략 미정의
- **위치**: 전체
- **문제**: 모바일 앱 테스트 방안 없음
- **영향**: 품질 보증 불가, 회귀 버그 위험
- **개선안**:
  ```
  테스트 전략:
  - Unit: Jest + React Native Testing Library
  - E2E: Detox 또는 Maestro
  - 디바이스 테스트: BrowserStack App Live
  ```

#### M-6. Deep Link 스키마 중복 위험
- **위치**: Section 6 - 앱스토어 요구사항
- **문제**: `spotfinder://` 스키마가 다른 앱과 충돌 가능
- **영향**: 딥링크 하이재킹 위험
- **개선안**:
  ```
  - Universal Links (iOS) + App Links (Android) 우선 사용
  - 도메인 검증: .well-known/apple-app-site-association
  - 도메인 검증: .well-known/assetlinks.json
  ```

#### M-7. 백그라운드 SSE 처리 미정의
- **위치**: Section 2 - SSE 스트리밍
- **문제**: 앱이 백그라운드일 때 SSE 연결 처리 방안 없음 (Phase 2이지만 중요)
- **영향**: 채팅 메시지 유실, 배터리 드레인
- **개선안**:
  ```
  - 포그라운드: SSE 연결 유지
  - 백그라운드: 연결 해제 + 푸시 알림으로 대체
  - 재연결: 앱 포그라운드 시 마지막 메시지 ID로 재요청
  ```

#### M-8. API Rate Limiting 모바일 대응 미정의
- **위치**: 백엔드 참조 (src/middleware/core/rate_limiter.py)
- **문제**: 현재 Rate Limit (분당 30회) 모바일 사용 패턴에 적합한지 검토 없음
- **영향**: 빠른 탐색 시 Rate Limit 도달
- **개선안**:
  ```
  모바일 고려 Rate Limit:
  - 인증된 사용자: 분당 60회
  - 비인증 사용자: 분당 20회
  - 푸시 알림 요청: 분당 10회 제한
  ```

---

### 🟢 Minor (개선 제안)

#### m-1. 타임라인 모호
- **위치**: Section 10 - 수정된 구현 우선순위
- **문제**: "Week 1", "Week 2" 등 상대적 시점만 있고 시작일 없음
- **개선안**: 실제 시작 예정일 명시 또는 스프린트 기반 계획

#### m-2. 접근성(A11y) 미언급
- **위치**: 전체
- **문제**: 모바일 접근성 가이드라인 없음
- **개선안**:
  - iOS: VoiceOver 지원
  - Android: TalkBack 지원
  - 최소 터치 타겟 44x44pt

#### m-3. 다국어 지원 범위 미명시
- **위치**: Section 2 - localStorage
- **문제**: 웹은 ja/en/ko 지원, 모바일 앱도 동일한지 불명확
- **개선안**: 지원 언어 명시 (일본어, 영어, 한국어)

---

## 누락된 요구사항

| ID | 요구사항 | 권장 우선순위 |
|----|---------|--------------|
| NEW-1 | 푸시 토큰 저장 DB 스키마 | P0 |
| NEW-2 | Supabase OAuth 리다이렉트 URI 등록 | P0 |
| NEW-3 | 개인정보처리방침/이용약관 페이지 | P0 |
| NEW-4 | Expo SDK 버전 명시 | P1 |
| NEW-5 | 앱 크래시 리포팅 (Sentry) | P1 |
| NEW-6 | 앱 버전 관리 API | P1 |
| NEW-7 | Universal Links 설정 | P1 |
| NEW-8 | 테스트 전략 (Unit/E2E) | P2 |
| NEW-9 | 접근성 가이드라인 | P2 |

---

## 리스크 매트릭스

| 리스크 | 발생 확률 | 영향도 | 대응 방안 |
|--------|----------|--------|----------|
| Naver Maps SDK 미지원 | 확정 | 고 | Google Maps 또는 WebView 대안 검토 |
| OAuth 제공자 장애 | 중 | 고 | 게스트 모드 fallback 필요 |
| 앱스토어 심사 거절 | 중 | 고 | 개인정보처리방침, 권한 설명 사전 준비 |
| Expo EAS 빌드 장애 | 저 | 중 | 로컬 빌드 fallback (expo-dev-client) |
| Rate Limit 과다 도달 | 중 | 중 | 모바일 전용 Rate Limit 정책 |
| 푸시 알림 전송 실패 | 중 | 중 | Expo Push API 재시도 로직 |

---

## 기술적 의존성 리스크

### 외부 서비스 의존도

| 서비스 | 역할 | 장애 시 영향 | 대안 |
|--------|------|-------------|------|
| Supabase Auth | 인증 | 로그인 불가 | 게스트 모드 |
| Expo Push | 푸시 알림 | 알림 미수신 | 인앱 알림 |
| Railway | API 서버 | 전체 기능 불가 | 캐시된 데이터 표시 |
| Naver Maps | 지도 | 지도 표시 불가 | Google Maps |

---

## 권장 조치

### 즉시 조치 (Critical) - 구현 시작 전 필수
1. ❗ 푸시 알림 백엔드 엔드포인트 설계 (C-1)
2. ❗ 푸시 토큰 DB 스키마 정의 (C-2)
3. ❗ Naver Maps 대안 결정 (C-3)
4. ❗ OAuth 리다이렉트 URI 등록 절차 문서화 (C-4)
5. ❗ 개인정보처리방침/이용약관 페이지 준비 (C-5)

### 구현 전 조치 (Major) - Week 1 완료 전 필수
1. ⚠️ Expo SDK 버전 결정 (M-1)
2. ⚠️ 오프라인 캐싱 상세 설계 (M-2)
3. ⚠️ Sentry 크래시 리포팅 계획 (M-4)
4. ⚠️ Universal Links 설정 계획 (M-6)

### 가능하면 조치 (Minor)
1. 💡 접근성 가이드라인 추가 (m-2)
2. 💡 다국어 지원 범위 명시 (m-3)

---

## 다음 단계

✅ **Critical 이슈 해결 후**: `/implement` 명령으로 구현을 시작하세요.

> 💡 C-1 ~ C-5 이슈가 모두 해결되면 구현을 시작해도 좋습니다.
> Major 이슈는 Phase 1 (Week 1) 중 병행 수정 가능합니다.

---

## Appendix: 코드 레벨 확인 결과

### 백엔드 현황
- `src/api/routes/chat.py`: SSE 스트리밍 지원 확인 ✅
- `src/api/routes/notifications.py`: **미존재** ❌
- `src/middleware/core/rate_limiter.py`: 분당 30회 제한 확인

### 프론트엔드 현황
- `frontend/src/lib/supabase/client.ts`: 웹 전용 (@supabase/ssr)
- 모바일용 Supabase 클라이언트 필요 (@supabase/supabase-js + AsyncStorage)

### 필요한 백엔드 추가 작업
```python
# src/api/routes/notifications.py (신규 생성 필요)
- POST /api/v1/push/register
- POST /api/v1/push/send
- GET /api/v1/app/version (강제 업데이트 체크용)
```
